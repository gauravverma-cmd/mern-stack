import Button from "./components/button/button";
import Header from "./components/header/header";
const App = () => {
  return(
  // So in this code we study that how we can attached the css file and use the module css..
    <div>
      <Header />
     <Button />
    </div>
  ) 
};

export default App;
